name: Publish VS Code Extension

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'   # triggers on: git tag v1.0.0 && git push --tags

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/vscode-extension

    steps:
      - uses: actions/checkout@v4

      # Copy the bundled skill files into the extension directory so vsce packages them.
      - name: Copy Velocity skill files into extension
        run: |
          mkdir -p skills/init skills/sync skills/validate
          mkdir -p templates/local-install
          cp ../../core/skills/init/SKILL.md      skills/init/SKILL.md
          cp ../../core/skills/sync/SKILL.md      skills/sync/SKILL.md
          cp ../../core/skills/validate/SKILL.md  skills/validate/SKILL.md
          cp ../../core/templates/local-install/README.md   templates/local-install/README.md
          cp ../../core/templates/local-install/manifest.yml templates/local-install/manifest.yml
        working-directory: packages/vscode-extension

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: packages/vscode-extension/package-lock.json

      - run: npm ci

      - name: Build extension
        run: npm run build

      - name: Publish to VS Code Marketplace
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: npx vsce publish --pat "$VSCE_PAT"
