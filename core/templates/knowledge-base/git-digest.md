# Git Digest — {Month YYYY}

> Auto-generated by `/ingest`. Do not edit manually — regenerate with `/ingest` or `/sync`.

## Period: {YYYY-MM-DD} to {YYYY-MM-DD}

---

## Volume

- Total commits: {N}
- Significant commits included below: {N}
- Active contributors: {N}

---

## Change Hotspots

Top areas by commit activity this period:

| Area               | Commits | Notes                                            |
| ------------------ | ------- | ------------------------------------------------ |
| {directory/module} | {N}     | {1-line note on what changed and why it matters} |
| {directory/module} | {N}     |                                                  |
| {directory/module} | {N}     |                                                  |

---

## Significant Changes

Commits with architectural, breaking, or notable impact:

| Hash     | Date         | Summary                                                 |
| -------- | ------------ | ------------------------------------------------------- |
| `{hash}` | {YYYY-MM-DD} | {feat/breaking/migration/refactor/arch: commit subject} |
| `{hash}` | {YYYY-MM-DD} |                                                         |

Significance criteria: `feat:`, `breaking:`, `migration:`, `refactor:`, `arch:`, `perf:`, `security:` prefixes; changes to `migrations/`, `schema/`, `api/`, `events/`, `domain/` directories.

---

## Patterns

{1–3 sentences summarising observed patterns: areas of active change, potential architectural shifts, stability signals, or notable absences of change. Example: "Heavy activity in the payments service around settlement flow — two schema migrations in four weeks suggest active data model iteration. No changes to the auth module this period."}

---

## Notes

{Any manually added context — incidents that explain a burst of activity, post-incident cleanup, major feature launch, etc. Leave blank if auto-generated with no manual context.}
